import { Module } from '@nestjs/common'; import { MongooseModule } from '@nestjs/mongoose'; import { CouriersModule } from '../couriers/couriers.module'; import { BatchesController } from './batches.controller'; import { HealthController } from './health.controller'; import { OrdersController } from './orders.controller'; import { OrdersService } from './orders.service'; import { Batch, BatchItem, BatchItemSchema, BatchSchema } from './schemas/batch.schema'; import { Order, OrderSchema } from './schemas/order.schema'; import { TrackingEvent, TrackingEventSchema } from './schemas/tracking-event.schema';
@Module({ imports: [CouriersModule, MongooseModule.forFeature([{ name: Order.name, schema: OrderSchema }, { name: TrackingEvent.name, schema: TrackingEventSchema }, { name: Batch.name, schema: BatchSchema }, { name: BatchItem.name, schema: BatchItemSchema }])], controllers: [OrdersController, BatchesController, HealthController], providers: [OrdersService] }) export class OrdersModule {}
